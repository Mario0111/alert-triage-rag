# --- Project-specific ---
# Persisted vector store (rebuild with ingest.py)
chroma_db/

# Secrets — never commit the Anthropic API key
.env
.env.*

# ATT&CK bundle: large (~51 MB), public, regenerable — not vendored.
# Download it with the command in the README (see "Setup").
corpus/attack/*.json

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so

# Virtual environments
.venv/
venv/
env/
ENV/

# Packaging / build
build/
dist/
*.egg-info/
.eggs/

# Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/

# Model / HF caches if pointed locally
.huggingface/

# Local dev/test scaffolding — kept out of the repo while building.
tests/
pytest.ini
requirements-dev.txt
dump_stix.py

# --- Editors / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
