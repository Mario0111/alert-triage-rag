# alert-triage-rag — fixed stack (see CLAUDE.md). Pins are tested starting
# points; bump deliberately. Python 3.11+.

# Local embeddings: bge-small-en-v1.5 via sentence-transformers (no API calls).
# Pulls in torch + transformers transitively (left unpinned — large, platform
# specific).
sentence-transformers==3.3.1

# Vector store, persisted to ./chroma_db
chromadb==0.5.23

# Generation via the Anthropic API (Claude)
anthropic==0.42.0

# Output validation — the schema.py contract
pydantic==2.10.4
